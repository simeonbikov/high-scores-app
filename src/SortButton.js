// import { useState } from "react";
// import allCountryScores from "./scores";

// function SortButton() {
//   const sortedDescending = [...allCountryScores].sort((a, b) =>
//     a.name > b.name ? 1 : b.name > a.name ? -1 : 0
//   );
//   const sortedAscending = [...allCountryScores].sort((a, b) =>
//     a.name < b.name ? 1 : b.name < a.name ? -1 : 0
//   );
//   const [sortedScores, setSortedScores] = useState(sortedDescending);

//   return (
//     <button className="btn" onClick={() => setSortedScores(sortedAscending)}>
//       Sort ↑↓
//     </button>
//   );
// }

// export default SortButton;
